# Install this context pack

Copy the files in this archive into the repository root, preserving directories.

This pack adds documentation only; it does not intentionally modify production Python code.

After installing, ask Codex:

> Read AGENTS.md and the documents it references. Inspect the current repository and tell me where the documentation disagrees with the current code. Do not modify code yet.

If the repository has advanced beyond the supplied v8 snapshot, update `ARCHITECTURE.md` and the active v9 plan before implementation.
